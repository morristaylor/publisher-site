{
  "name": "publisher-site",
  "version": "1.0.0",
  "description": "Book publisher sample site",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taylor Morris",
  "license": "ISC",
  "dependencies": {
    "normalize.css": "^8.0.0"
  }
}
